/**
 * Shared types/interfaces for the app.
 * For DB types use supabase/types.ts (generated from Supabase CLI).
 * Add app-specific types here as needed.
 */

// Placeholder; extend with JSDoc or migrate to TypeScript when needed
export default {};
